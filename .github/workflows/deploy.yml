name: 🚀 Deploy to InfinityFree

on:
  push:
    branches: [main]
  workflow_dispatch: # Tombol manual deploy di GitHub

jobs:
  deploy:
    name: 📦 Build & Deploy
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout kode dari repository
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      # 2. Setup PHP 8.3
      - name: 🐘 Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: mbstring, xml, ctype, json, bcmath, curl

      # 3. Cache Composer dependencies
      - name: 💾 Cache Composer
        uses: actions/cache@v4
        with:
          path: vendor
          key: composer-${{ hashFiles('composer.lock') }}
          restore-keys: composer-

      # 4. Install Composer dependencies (production only)
      - name: 📦 Install Composer Dependencies
        run: composer install --no-dev --optimize-autoloader --no-interaction --prefer-dist

      # 5. Setup Node.js
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      # 6. Install NPM dependencies
      - name: 📦 Install NPM Dependencies
        run: npm ci

      # 7. Build Vite + Tailwind assets
      - name: 🔨 Build Vite Assets
        run: npm run build

      # 8. Siapkan struktur folder sesuai InfinityFree
      - name: 📁 Prepare Deployment Structure
        run: |
          mkdir -p deploy/24.12.3305_LaravelApp

          # ===== Public files → deploy/ (htdocs/) =====
          # index.php yang sudah dimodifikasi path-nya
          cp index.php.production deploy/index.php
          # .htaccess untuk rewrite rules
          cp public/.htaccess deploy/.htaccess
          # Vite build output
          cp -r public/build deploy/build
          # Static assets
          [ -d public/assets ] && cp -r public/assets deploy/assets || true
          [ -d public/images ] && cp -r public/images deploy/images || true
          [ -f public/favicon.ico ] && cp public/favicon.ico deploy/favicon.ico || true
          [ -f public/robots.txt ] && cp public/robots.txt deploy/robots.txt || true

          # ===== Core Laravel → deploy/24.12.3305_LaravelApp/ =====
          cp -r app deploy/24.12.3305_LaravelApp/app
          cp -r bootstrap deploy/24.12.3305_LaravelApp/bootstrap
          cp -r config deploy/24.12.3305_LaravelApp/config
          cp -r database deploy/24.12.3305_LaravelApp/database
          cp -r resources deploy/24.12.3305_LaravelApp/resources
          cp -r routes deploy/24.12.3305_LaravelApp/routes
          cp -r vendor deploy/24.12.3305_LaravelApp/vendor
          cp artisan deploy/24.12.3305_LaravelApp/artisan
          cp composer.json deploy/24.12.3305_LaravelApp/composer.json
          cp composer.lock deploy/24.12.3305_LaravelApp/composer.lock

          # ===== Storage structure (buat folder kosong) =====
          mkdir -p deploy/24.12.3305_LaravelApp/storage/app/public
          mkdir -p deploy/24.12.3305_LaravelApp/storage/framework/cache/data
          mkdir -p deploy/24.12.3305_LaravelApp/storage/framework/sessions
          mkdir -p deploy/24.12.3305_LaravelApp/storage/framework/views
          mkdir -p deploy/24.12.3305_LaravelApp/storage/logs

          # Buat .gitkeep agar folder tidak kosong saat FTP
          touch deploy/24.12.3305_LaravelApp/storage/app/public/.gitkeep
          touch deploy/24.12.3305_LaravelApp/storage/framework/cache/data/.gitkeep
          touch deploy/24.12.3305_LaravelApp/storage/framework/sessions/.gitkeep
          touch deploy/24.12.3305_LaravelApp/storage/framework/views/.gitkeep
          touch deploy/24.12.3305_LaravelApp/storage/logs/.gitkeep

          # ===== 🧹 Clean vendor folder (Optimize size & inodes for InfinityFree) =====
          echo "🧹 Cleaning non-essential vendor files..."
          find deploy/24.12.3305_LaravelApp/vendor -type d -name "tests" -exec rm -rf {} +
          find deploy/24.12.3305_LaravelApp/vendor -type d -name "test" -exec rm -rf {} +
          find deploy/24.12.3305_LaravelApp/vendor -type d -name "docs" -exec rm -rf {} +
          find deploy/24.12.3305_LaravelApp/vendor -type d -name "spec" -exec rm -rf {} +
          find deploy/24.12.3305_LaravelApp/vendor -type d -name ".github" -exec rm -rf {} +
          find deploy/24.12.3305_LaravelApp/vendor -type f -name "*.md" -delete
          find deploy/24.12.3305_LaravelApp/vendor -type f -name "*.txt" -delete
          find deploy/24.12.3305_LaravelApp/vendor -type f -name "LICENSE" -delete
          find deploy/24.12.3305_LaravelApp/vendor -type f -name "LICENSE.txt" -delete
          find deploy/24.12.3305_LaravelApp/vendor -type f -name "CHANGELOG" -delete

          echo "✅ Deployment structure ready!"
          echo "📊 Total size:"
          du -sh deploy/
          echo "📊 Total inodes:"
          find deploy/ -type f | wc -l

      # 9. Deploy ke InfinityFree via FTP
      - name: 🚀 Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./deploy/
          server-dir: /htdocs/
          dangerous-clean-slate: false
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            24.12.3305_LaravelApp/.env
            uploads/**